// Example configuration file - Copy this to config.js and add your actual values
const config = {
    oauth: {
        client_id: 'YOUR_CLIENT_ID_HERE',
        scopes: [
            'https://www.googleapis.com/auth/calendar.readonly'
        ]
    }
};

export default config; 